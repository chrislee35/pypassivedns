============
Installation
============

At the command line::

    $ easy_install pypassivedns

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pypassivedns
    $ pip install pypassivedns
